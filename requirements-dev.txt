ruff>=0.16.0
pre-commit>=4.0
pip-audit>=2.10.1
pyinstaller>=6.21.0
# Starlette's TestClient (tests/security_test.py) needs an httpx transport.
# Dev-only: it is not in requirements.txt, so it never reaches a build.
httpx2>=2.9.1
